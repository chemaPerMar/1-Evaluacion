package ejercicio_4;

public class Ejercicio_4 {
	
	public static void main(String[] args) {
		
		boolean z = 2 < 1;							//	(le asigna el valor de false a la variable booleana z)
		int a = a + 1;								//	(no puede utilizar la variable a para la asignación porque no ha sido inicializada con anterioridad, pero si ya lo estuviera funcionaría, y su efecto sería el de sumarle 1 a su valor actual)
	//	double Math.sqrt(3) = 32717 ; 					(no es válida porque el nombre de la variable no puede ser una llamada a una función)
	//	char 'x'='y'; 									(el nombre de la variable no puede tener ' )
		char x = 'y';								//	(asigna el valor 'y' a la variable de tipo char x)
	//	int a = b; 										(b no esta inicializada, si lo estuviera, asignaría el valor de la variable b a la variable a)
		int precio= precio - precio*(30/100);		//	(en caso de estar la variable precio inicializada, asignaría la diferencia entre el valor actual y el valor actual multiplicado por 0.3)
	}

}
